# 🗺️ IMPACT-MAP-42 — نقشهٔ اثرِ پنهان‌سازیِ تب‌ها روی زنجیرهٔ تولید (فقط‌خواندنی)

> **دامنه:** فقط گزارش — صفر تغییر منطق/API (خط قرمز پرامپت). تمام شماره‌خط‌ها مستند به HEADِ `c68adc5`
> (پس از QC-EDITABLE-43 و ROLE-CLEANUP-44) و با grep تأیید شده‌اند — صفر حدس.
> **زمینهٔ استقرار:** کیان‌فولاد با `hidden_tabs: ["warehouse","finance","sales","purchase","genealogy","balance"]`
> (تنظیم tenant — گیتِ GO-LIVE-32a). گیت فقط UI است: سرور فقط pass-through می‌کند و هیچ endpoint بسته نمی‌شود
> (server.js:476-506 تعریف و پاک‌سازی شناسه‌ها؛ 1063 و 1180 پاس‌ترو؛ index.html:5093 `hiddenTabs32` و 5129 `applyHiddenTabs32` حذف DOM).

---

## الف) جدولِ حلقه‌های یتیم (منطق زنده — مسیر ورود داده پنهان/غایب)

| # | حلقه | WRITE (مسیر ورود داده) | READ (مصرف‌کننده‌های زنده) | وضعیت با hidden_tabs فعلی |
|---|------|------------------------|---------------------------|----------------------------|
| ۱ | **GRN خرید شمش (رسید با کد هیت)** | `POST /api/purchase/receipts` (server.js:6805) — هیت برای شمش الزامی (:6842)؛ آیتم BILLET خودکار (انه‌ده، :6387)؛ سند مالی خودکار (:6857-6869) | فرم «آزمون ورودی» QC (index.html:2230-2260 از `receipts_ref` server.js:7456)؛ ارزیابی تأمین‌کنندگان خرید (server.js:7226) | 🔴 **یتیم** — WRITE در تبِ purchase (index.html:1729) که پنهان است؛ هیچ نقش دیگری رسید نمی‌تواند ثبت کند |
| ۲ | **آزمون ورودی QC (شمش→هیت→QC)** | `POST /api/qcpro/incoming` (server.js:7563) با **پیش‌نیاز GRN** (:7569 «آزمون ورودی باید به رسید خرید (GRN) ارجاع داشته باشد») | MTC (7820-7823: بدون آزمون تأییدشده، MTC صادر نمی‌شود)؛ قرنطینهٔ خودکار رد QC (:7636-7641) | 🔴 **قطع سرتاسری** — چون حلقهٔ ۱ پنهان است، آزمون ورودی هم ناممکن است؛ تب quality خودش باز است (فقط محروم از داده) |
| ۳ | **بندیل/هیت per سایز (ورودی MTC)** | **هیچ نویسندهٔ وب‌UI** — فقط `/api/ingest` عمومی (server.js:1709-1719 → mergeIntoLive :280-294، TABLES :36) از اپ legacy؛ demo-seed (tools/demo-seed.js:36) | `qcHeatsOfSize24b` (5798-5802) → صدور MTC (:7820-7821 «هیت مرجع برای سایز … یافت نشد»)؛ گارد حواله NCR (5805-5808، 5961-5962)؛ جست‌وجوی ردیابی (8051، index.html:2368) | 🔴 **یتیم ساختاری** — تولید وب `heat_number` را در production_logs می‌نویسد (server.js:4089، فرم index.html «fpHeat») ولی MTC فقط از `rebar_bundles` می‌خواند؛ دادهٔ هیتِ تولید هرگز به MTC نمی‌رسد |
| ۴ | **تناژ شمش ورودی/مصرفی (بچ‌های billets)** | **هیچ نویسندهٔ فعالی در سرور** — فقط `/api/ingest` (TABLES شامل `billets`، server.js:36,280-294)؛ PURGE-36 فایل را صفر کرده | KPI «تناژ شمش ورودی» (index.html:4281 از `/api/billets` server.js:8026)؛ میانگین وزن شمش COLDSTART (1949)؛ کالیبراسیون انرژی ۹۰روزه (2671-2682)؛ بهای واقعی مالی (2736)؛ `/api/balance/:heat` (buildBalance 447-452، مسیر :8052) | 🔴 **یتیم** — تب balance (index.html:1715) پنهان و به‌علاوه هیچ فرم وبی برای بچ شمش وجود ندارد؛ KPI همیشه صفر |
| ۵ | **پرچم کمبود شمش در APS گرم** | دادهٔ ورودی از انبار: `live.billets` (3310-3313 میانگین) + آیتم‌های شمش/بیلت انبار (3300-3306) — که فقط با رسید خرید (:6857+) یا رسید انبار پنهان پر می‌شوند | موتور برنامه‌ریزی: سقف‌گذاری تناژ + پرچم (:3450-3452)؛ در شروع سرد فقط هشدار و «شمش در دسترس فرض شد» (:1787، 3455) | 🟠 **نیمه‌یتیم** — منطق زنده (server.js:3300-3464) ولی ورودی داده‌اش از تب‌های پنهان می‌آید؛ امروز همیشه شاخهٔ «پایهٔ مهندسی» اجرا می‌شود |
| ۶ | **انبار (رسید/حواله/اصلاح)** | `POST /api/inventory/receipt` (4820) / `/api/inventory/issue` (5110، push :5159) / transfer/adjustment/reserve | برنامه‌ریزی (بالا)؛ ستون «مسدود» قرنطینه QC (7636-7641)؛ سند مالی مصرف شمش (2344)؛ برگشت خرید (7113) | 🔴 **یتیم** — تب warehouse (index.html:1717) پنهان؛ خروج فروش هم به‌صورت سروری زنده است (:6001-6006) ولی بدون سفارش فروش هرگز رخ نمی‌دهد |
| ۷ | **مالی (سند دستی + مشاهده)** | موتور خودکار `finPostDoc` از همهٔ ماژول‌ها زنده است (شمش :2344، رسید خرید :6857-6869، تعداد/برگشت :7038-7086، NCR رد)؛ ثبت دستی و مشاهده در تب finance (index.html:1723) | BOM/بهای استاندارد/QC (bom_ref server.js:7458) | 🟠 **نیمه‌یتیم** — دفتر از سمت سرور پر می‌شود ولی کسی نمی‌تواند ببیند/سند دستی بزند؛ ریسک: سند بدون مشاهده = کنترل‌ناپذیری، نه قطع |
| ۸ | **فروش (سفارش/حواله)** | `POST /api/sales/orders` (5822) / `/api/sales/exits` (5945) با گارد NCR باز (5961-5962) و MTC الزامی صادرات (6125) | کاندیدهای MTC (8004-8010)؛ خروج انبار فروش (6001-6006) | 🔴 **یتیم** — تب sales (index.html:1726) پنهان ⇒ حواله‌ای ساخته نمی‌شود ⇒ فهرست MTC خالی و گاردهای 409 هرگز فعال نمی‌شوند |

**نکتهٔ امنیتی مشاهده‌شده (خارج از دامنهٔ این سند — صرفاً ثبت):** `/api/ingest` (server.js:1709) عمومی/بی‌احراز است (whitelist auth.js:157 `PUBLIC_API`) و می‌تواند به ۷ جدول legacy (شامل billets و rebar_bundles) رکورد تزریق کند — یعنی «تنها نویسندهٔ» حلقه‌های ۳ و ۴ در واقع یک endpoint باز است.

---

## ب) برای هر حلقهٔ یتیم: دو گزینهٔ پیاده‌سازی‌پذیر

> «الف» = بازگشتِ محدودِ همان زیر-قابلیت با **سوییچ tenant جدید** (بدون بازکردن کل تب)؛
> «ب» = انتقالِ فیلدِ ورودی به فرم‌های **بازِ** تولید/QC. حجم کار: S ≤ روزی، M = ۱-۳ روز کاری، L > ۳ روز.

### حلقهٔ ۱ (GRN خرید شمش)
- **الف)** سوییچ tenant مثل `allow_grn_entry: 'qc'` — فرمِ «ثبت رسید خرید» (فقط رسید، نه کل خرید) به‌صورت پنل داخلی در تب quality رندر شود؛ endpoint همان `POST /api/purchase/receipts` (6805) با گارد نقش موجود (انباردار/خرید/qc). فایل: index.html (پنل جدید ~۱۲۰ خط + وایرینگ)، server.js:0 (endpoint هست). **حجم: M**.
- **ب)** در فرم «ثبت آزمون ورودی» (index.html:8624-8640) حالت «رسید ندارد» با واردکردن دستی تأمین‌کننده/تناژ اضافه شود و سرور مسیر بدون-GRN را بپذیرد (تغییر :7569 و validation های 7563-7615). **حجم: M** — ⚠ ریسک: تضعیف زنجیرهٔ مالی رسید؛ نیازمند audit.

### حلقهٔ ۲ (آزمون ورودی بدون GRN)
- **الف)** سوییچ `incoming_without_grn: true` — پذیرش آزمون با هیتِ دستی (بازطراحی همان validation سرور). **حجم: S-M**.
- **ب)** اجرای حلقهٔ ۱ به هر شکل — آزمون همان فرم فعلی بدون تغییر کد کار می‌کند (GRN خودکار ظاهر می‌شود در `receipts_ref` :7456). **حجم: صفر کد** (فقط تنظیم tenant/dسترسی).

### حلقهٔ ۳ (هیت → MTC)
- **الف)** سوییچ `mtc_heat_source: 'production'` — `qcHeatsOfSize24b` (5798) به‌جای/به‌علاوهٔ `rebar_bundles` از `production_logs.heat_number` (فیلتر سایز از product_id مثل RB-16) هیت جمع کند؛ همان Set semantics. فایل/تابع: server.js:5798-5802 فقط (~۸ خط) + تست. **حجم: S**.
- **ب)** ثبت بندیل در فرم تولید (بندیل‌ساز ساده per هیت/سایز/وزن → `rebar_bundles` + `POST /api/bundles` جدید) — دوباره‌سازی زیر-قابلیت genealogy در تب production. **حجم: M-L** (فرم + endpoint + گاردهای تکراری بودن).

### حلقهٔ ۴ (بچ شمش / تناژ ورودی و مصرفی)
- **الف)** سوییچ `billet_batch_entry: 'production'` — فرم کوچک «بچ شمش ورودی» (هیت، وزن اولیه، تعداد) در تب production که به `live.billets` اضافه کند (endpoint `POST /api/entry/billets` جدید ~۳۰ خط؛ READ ها همه از قبل زنده‌اند: 1949/2671/2736/8026/8052). **حجم: S-M**.
- **ب)** اگر رسید خرید (حلقهٔ ۱) فعال شود، از ردیف‌های رسید (kind=billet, qty) میانگین/تناژ استخراج شود و `billets` فقط legacy بماند — نیازمند تطبیق 4 reader (1949، 2671، 2736، 3310) به reading از purchase_receipts. **حجم: M**.

### حلقهٔ ۵ (پرچم کمبود شمش APS)
- **الف)** سوییچ `aps_billet_source: 'grn'` — شمارش موجودی شمش از GRN منهای مصرف (به‌جای inventory/billets) در 3300-3313. **حجم: M**.
- **ب)** اجرای حلقهٔ ۴ (هر شکل) — موتور بدون تغییر کد دادهٔ واقعی می‌گیرد و پرچم واقعی می‌زند (3450-3452). **حجم: صفر کد پس از حلقهٔ ۴**.

### حلقه‌های ۶/۷/۸ (انبار/مالی/فروش) — تصمیم محیطی، نه کدی
- **الف)** حذف سه کلید از `hidden_tabs` در tenant.json کیان‌فولاد (پیکربندیِ خالص؛ گیت فقط UI است — server.js:476). **حجم: صفر کد**.
- **ب)** در صورت اصرار بر پنهان‌ماندن: هیچ راه امنی برای «فقط یک تکه» نیست چون WRITE و READ زنجیره‌ای است (فروش→انبار→مالی موتور واحد 2344/6001-6006/6857)؛ حداقلِ توصیه‌شده: تب finance فقط‌خواندنی برای مدیر باز شود تا اسناد خودکارِ درحال‌انباشته دیده شوند (سقف نقش role_caps موجود).

---

## ج) حکمِ صریح برای go-live کیان‌فولاد

**خیر — با hidden_tabs فعلی، زنجیرهٔ میلگرد «بسته» نیست.** زنجیره در سه نقطه می‌شکند:

1. **شمش ورودی ثبت نمی‌شود** (رسید خرید پنهان = حلقهٔ ۱) ⇒ **آزمون ورودی QC ناممکن** (پیش‌نیاز GRN در server.js:7569) ⇒ **MTC هرگز صادر نمی‌شود** (7820-7823).
2. **حتی اگر آزمون بود، MTC باز هم می‌شکست** — چون هیتِ مرجع فقط از `rebar_bundles` می‌آید (5798) و هیچ مسیر وبی برای ساخت بندیل نیست (حلقهٔ ۳)؛ هیتِ ثبت‌شدهٔ تولید (4089) بلااستفاده می‌ماند.
3. **حواله/فاکتور فروش و دفتر مالی قابل مشاهده نیستند** (حلقه‌های ۷/۸) و پرچم کمبود شمش APS همیشه به «پایهٔ مهندسی» سقوط می‌کند (3455).

آنچه **سالم کار می‌کند**: ثبت تولید (با هیت)، ضایعات، توقفات، PM، برنامه‌ریزی در حالت شروع سرد، QC-PRO مستر گرید/مشخصات/مستر پارامترهای 43، و NCR (scope هیت/رسید — فقط بندیل‌scope بی‌اثر است).

### حداقلِ تغییرِ امن برای go-live (پیشنهاد، به ترتیب هزینه)
1. **صفرِ کد:** بازگرداندن `purchase` و `warehouse` به visible در tenant.json (حلقهٔ ۱ + ۶ باز؛ آزمون QC و پرچم کمبود با تنظیم tenant به زنجیره وصل می‌شوند) — اگر پنهان‌سازی تجاری است، حداقل نقشِ qc/انباردار این دو تب را ببیند.
2. **یک تغییرِ S:** سوییچ `mtc_heat_source: 'production'` (حلقهٔ ۳-الف؛ server.js:5798) تا MTC از هیتِ تولیدِ ثبت‌شده کار کند حتی بدون بندیل.
3. **اختیاری M:** سوییچ `billet_batch_entry` (حلقهٔ ۴-الف) برای KPI تناژ ورودی و کالیبراسیون انرژی با دادهٔ واقعی.
4. تب‌های finance/sales/genealogy/balance: تصمیم تجاری؛ تا باز نشدن، اسناد خودکار مالی انباشته می‌شوند و MTC بدون حواله فقط با `order_id` (server.js:7790) کار می‌کند.

---

## د) جدول جمع‌بندی الف–د (مرجع سریع)

| کلید hidden_tabs | WRITE پنهان‌شده | READ یتیم | حلقه‌های متأثر | حکم |
|---|---|---|---|---|
| `purchase` (1729) | GRN + هیت (6805/6842) | آزمون ورودی QC (7456/7569) | ۱،۲،۵ | 🔴 بحرانی |
| `warehouse` (1717) | رسید/حواله/اصلاح انبار (4820/5110) | APS شمش (3300)، قرنطینه QC، مالی (2344) | ۵،۶ | 🔴 بحرانی |
| `sales` (1726) | سفارش/حواله (5822/5945) | کاندید MTC (8004)، خروج انبار (6001) | ۸ | 🔴 بحرانی برای چرخهٔ فروش؛ ثبت تولید مستقل است |
| `finance` (1723) | سند دستی + مشاهده | خودکار سروری زنده (2344/6857/7038) | ۷ | 🟠 ریسک شفافیت |
| `genealogy` (2368) | هیچ (فقط‌خواندن؛ نویسنده = ingest/demo — buildGenealogy server.js:443-448) | ردیابی هیت (8051) | ۳ | 🔴 ساختاری (نویسندهٔ وب وجود ندارد) |
| `balance` (1715) | هیچ (نویسنده = ingest) | KPI تناژ (4281)، کالیبراسیون (2671)، بهای واقعی (2736)، بالانس هیت (8052) | ۴ | 🔴 ساختاری |

> **سلب مسئولیت روش‌شناختی:** این سند فقط نقشهٔ اثر است؛ هیچ گزینه‌ای بدون تصمیم صریح مدیرعامل (CEO) اجرا نشده و هیچ کدی در این فاز تغییر نکرده است (به‌جز همین فایل مستندات).
